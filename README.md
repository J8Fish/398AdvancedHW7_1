# Coverage.py

## Quick start

Use ```coverage run``` to run your program.
```bash
coverage run --source=. -m unittest
```

To see the result:
```bash
coverage report
```

To show the missing line:
```bash
coverage report -m
```
